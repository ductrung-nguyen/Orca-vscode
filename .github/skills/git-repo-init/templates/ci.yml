# Minimal GitHub Actions CI Workflow
# Generated by git-repo-init skill
#
# This is a language-agnostic CI workflow with placeholders.
# Replace the placeholder steps with your project's actual commands.

name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Setup runtime environment
      # PLACEHOLDER: Uncomment and configure for your language
      #
      # For Node.js:
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      #
      # For Python:
      # - name: Setup Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      #
      # For Go:
      # - name: Setup Go
      #   uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.22'
      #
      # For Rust:
      # - name: Setup Rust
      #   uses: dtolnay/rust-toolchain@stable

      # Step 3: Install dependencies
      # PLACEHOLDER: Replace with your project's install command
      - name: Install dependencies
        run: |
          echo "TODO: Add your install command here"
          # Examples:
          # npm ci
          # pip install -r requirements.txt
          # go mod download
          # cargo fetch

      # Step 4: Run linting
      # PLACEHOLDER: Replace with your project's lint command
      - name: Lint
        run: |
          echo "TODO: Add your lint command here"
          # Examples:
          # npm run lint
          # ruff check .
          # golangci-lint run
          # cargo clippy

      # Step 5: Run tests
      # PLACEHOLDER: Replace with your project's test command
      - name: Test
        run: |
          echo "TODO: Add your test command here"
          # Examples:
          # npm test
          # pytest
          # go test ./...
          # cargo test

      # Step 6: Build (optional)
      # PLACEHOLDER: Replace with your project's build command
      # - name: Build
      #   run: |
      #     echo "TODO: Add your build command here"
      #     # Examples:
      #     # npm run build
      #     # go build ./...
      #     # cargo build --release
